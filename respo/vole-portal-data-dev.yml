# service配置------------------------------------------------------------------------------------------------------------------------------------------------
service:
  server:
    port: 8510
    

spring:
  datasource:
    url: jdbc:mysql://192.168.2.219:3306/vole?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    filters: mergeStat
    connection-properties: config.decrypt=true
    test-while-idle: true
    validation-query: select 'x'
    max-active: 100
    initial-size: 5

mybatis:
    type-aliases-package: com.github.vole.portal.data.mapper
    mapper-locations: classpath:mapper/*.xml



# 公用配置------------------------------------------------------------------------------------------------------------------------------------------------